## Adding `Dockerfile` for the MongoDB service

- Refer to the slides and add the `Dockerfile` for the `db` container.
- Our database service in this example is `mongodb` version `4.4.2`
- The port used `27017`
